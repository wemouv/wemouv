spring.application.name=wemouv

# MySQL
SPRING_DATASOURCE_URL=jdbc:mysql://82.165.152.149:3307/2025-D11-groupe2
SPRING_DATASOURCE_USERNAME=wemouv
SPRING_DATASOURCE_PASSWORD=fo87)kbf67
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# MAIL - Correspond aux clés ajoutées sur Render
spring.mail.host=${SPRING_MAIL_HOST}
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLED}

# JWT
jwt.secret=${JWT_SECRET}

# Logs
logging.level.root=INFO

server.port=${PORT:8080}